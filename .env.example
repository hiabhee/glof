# Local-only configuration. Do not commit real credentials.
GEE_PROJECT_ID=your-google-earth-engine-project-id

# Set this when a FastAPI service is running locally.
NEXT_PUBLIC_API_URL=http://localhost:8000
